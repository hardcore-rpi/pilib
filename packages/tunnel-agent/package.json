{
  "name": "pilib-tunnel-agent",
  "version": "1.0.1",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "bin": "dist/cli.js",
  "scripts": {
    "dev": "ts-node -P tsconfig.json src/cli.ts",
    "build": "rm -rf dist && tsc --outDir dist",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "ah-event-bus": "^1.0.8",
    "ah-logger": "^0.1.4",
    "js-yaml": "^4.1.0",
    "node-pty": "^0.10.0",
    "pilib-tunnel-core": "^2.0.0",
    "pilib-tunnel-node": "^2.1.1",
    "pilib-tunnel-ssh-protocol": "^1.0.0",
    "yargs": "^16.2.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.1",
    "@types/websocket": "^1.0.2",
    "@types/yargs": "^16.0.1",
    "typescript": "^4.2.4"
  }
}
