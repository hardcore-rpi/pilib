{
  "name": "pilib-tunnel-ssh-protocol",
  "version": "1.0.2",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist/* && tsc --outDir dist",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "pilib-tunnel-core": "^2.0.0"
  },
  "dependencies": {
    "ah-event-bus": "^1.0.8"
  },
  "devDependencies": {
    "pilib-tunnel-core": "^2.0.1",
    "typescript": "^4.2.4"
  }
}
