{
  "name": "pilib-tunnel-browser",
  "version": "1.0.1",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node -P tsconfig.json src/cli.ts",
    "build": "rm -rf dist && tsc --outDir dist",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "pilib-tunnel-core": "^1.0.1"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  }
}
