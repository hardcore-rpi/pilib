{
  "name": "pilib-tunnel-browser",
  "version": "1.0.0",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node -P tsconfig.json src/cli.ts",
    "build": "rm -rf dist && tsc --outDir dist",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "pilib-tunnel-core": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  }
}
